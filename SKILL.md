---
name: lvmaiya-product-deconstruction
description: "Evidence-led reverse engineering for any digital or AI product from screenshots, recorded UI flows, websites, prototypes, local artifacts, and official documentation. Use when Codex needs to analyze or audit a product's user journey, workflows, agents, tools, global context, assets, model choices, data flow, architecture, state inconsistencies, risks, or To-Be design; especially when the deliverable is a cited HTML report, journey map, Agent I/O contract, or architecture diagram."
---

# 绿麦芽的万能产品拆解大法宝

以可复核证据为先，拆解任何产品的用户体验、功能、Agent、工具、数据、资产与架构。输出要帮助产品经理、设计师和工程师区分“已经看到的系统”与“为解决问题建议建设的系统”。

## 先定边界

- 默认只读。浏览页面、查看截图、读取本地文件和官方公开资料可以；不得发送消息、触发生成、发布、删除、购买、充值、重试或覆盖资产，除非用户单独授权。
- 不读取或输出 Cookie、Token、密码、鉴权头、个人敏感信息或其他不在任务范围内的数据。
- 不把“思考完成”“规划完成”当作隐藏思维链；它们只能作为面向用户的判断摘要。
- 不把 Agent 的计划或口头宣告当作工具调用成功。只有可见结果卡、资产变化、任务/状态变化、错误、可复核工具结果或官方文档才能确认动作。
- 不把前端状态当作后端真相。聊天、画布、资产页、预览、任务面板不一致时，逐项记录冲突，不任选一方为事实。
- 只能把官方资料、产品页面和用户提供材料当作外部证据；不要用第三方猜测证实架构。

## 证据等级

为每个非平凡结论贴一个标签，并紧邻列出证据编号、页面原文、按钮、资产、错误或官方链接。

- `【已确认】`：页面、官方文档或可复核产物直接支持。
- `【合理推断】`：由两条或以上事实推导，说明推导链和替代解释。
- `【建议设计】`：为修复风险、提升可运营性或补齐能力提出；不要暗示已存在。
- `【未知】`：当前材料不足。说明需要查看什么才能确认。

不要把截图文件名、整理者注释或 Agent 名称以外的元数据当作唯一事实；优先记录页面上实际可见的内容。

## 工作流

### 1. 建立证据台账

1. 先列出全部输入：截图、录屏、网页、原型、日志、文档、CSV、已有报告与官方资料。
2. 给每项证据分配稳定编号，如 `S01`、`P03`、`D02`；记录时间顺序、可见范围和遮挡/缺页。
3. 逐张以足够清晰的倍率视觉检查所有截图；查看聊天、表单、按钮、画布、资产、任务和错误。文件名、目录名、CSV 注释和既有报告只能用于编目，绝不能替代图中内容或成为 Agent/状态的唯一依据。
4. 若页面可访问，按只读方式检查：任务标题、导航、模型选项、表单、Agent 身份、资产卡、历史/版本、预览、错误、余额和状态。不要执行有副作用的动作。
5. 记录“未能访问/未能确认”的证据，不以经验补齐。

详见 [references/evidence-and-output-schema.md](references/evidence-and-output-schema.md) 的取证字段与冲突登记格式。

### 2. 先还原用户旅程，再拆后台

按时间顺序输出每一阶段：

| 阶段 | 用户目标与动作 | 页面反馈 | 决策/分支 | 状态与资产变化 | 阻力 | 证据 |
|---|---|---|---|---|---|---|

至少检查：初始需求、参数/风格、满意/修改、方案选择、暂停/取消/返回、正常生成、依赖阻断、模型失败、余额不足、预览/编辑/下载/发布。没有证据的分支明确写 `【未知】`。

画用户旅程时仅使用三条泳道：**用户**、**产品界面**、**系统结果**。用菱形表示判断；主流程、修改流程、失败/中断流程必须分别可见。节点写入证据编号。

### 3. 识别实际出现的 Agent

- 只记录页面、官方资料或可复核产物支持的 Agent/系统角色。不要预设固定数量，也不要把“任务容器”自动等同于主控 Agent。
- 对每个角色记录：首次可见、触发者、接替谁、给谁交接、是否再次出现、哪些通常角色没有出现。
- 对每个 Agent 建立 I/O 契约。输入逐项检查六类：用户当前输入、用户长期信息、项目全局上下文、上游 Agent 输出、平台公共资产/知识、工具/运行时结果。
- 长期用户信息、Face ID、音色、历史资产、套餐、权限等只有可见证据才能标事实；否则推断或未知。

每张卡必须使用以下标题：

1. 核心目标
2. 触发条件
3. 输入信息（注明六类来源与证据等级）
4. 可观察判断（不还原隐藏思维链）
5. 调用工具（功能命名；不是官方工具名时明确标注）
6. 输出信息（用户回复、页面组件、结构化字段、视觉/音视频资产、状态、下游任务）
7. 全局上下文读写（字段、读/写、生产者、消费者、更新时机、证据）
8. 完成条件
9. 异常与重试
10. 尚未确认的问题

### 4. 拆工具、资产与全局上下文

- 使用功能名描述未公开的工具，例如“读取项目上下文”“生成角色主图”“检查余额”“写入分镜引用”。紧跟 `功能命名，并非官方工具名`。
- 为每个工具记录：关联 Agent、可见输入、可见结果、执行结论（已确认/仅计划/未知）。
- 不把全局上下文画成大黑箱。根据证据分别核查 User、ProjectConfig、Script、Character、Scene、Prop、Storyboard、Asset、WorkflowState、Billing、Evaluation 等上下文；这些是分析模板，不代表真实字段。
- 对每个资产记录生产者、版本、项目归属、被谁引用、完成度、预览/下载能力和冲突。不可见的 ID、存储地址和历史版本不要编造。
- 明确检查“上游修改是否让下游失效”“只写不读/只读不写”“口头完成但状态没更新”。

### 5. 按需拆产品架构

当用户要求完整架构时，依次覆盖：

1. 证据来源与缺口
2. 核心功能域
3. 五条并列的端到端流：用户交互、Agent 控制、工具调用、数据/全局上下文、图片/视频/音频资产
4. 分层架构：用户与渠道、工作台、应用、Agent/编排、工具/服务、模型、数据、知识、基础设施与治理
5. Agent—工具—上下文与数据生产者—消费者关系
6. 知识与公共资产：项目数据、专业知识、模型能力、风格/提示词、安全/版权/计费、私有资产隔离
7. 模型接入：只陈述页面可见模型；路由、成本、时长、失败率均无证据时标未知
8. 技术能力选型：先写需要的能力，再写可能选型和推荐方案；不要猜云厂商、语言、数据库或队列
9. 数据实体、ER 图、端到端时序图、产品全景架构图
10. As-Is、To-Be、风险、组件—证据追溯与待验证问题

架构图必须用图例区分：实线 `【已确认】`、虚线 `【合理推断】`、点线/特殊色 `【建议设计】`。箭头标注调用、读取、写入、事件、确认、资产引用或状态更新。把状态冲突和未完成校验画成问题节点。

### 6. 生成交付物

当用户要求 HTML、可视化页面或完整可交付报告时，交付一个独立 HTML 页面，且可在普通浏览器打开；其他情况下严格服从用户指定的交付格式，不因本 Skill 默认创建文件：

- 顶部给一句话执行摘要、证据范围和图例。
- 用 HTML 表格呈现旅程、Agent 契约、工具、上下文、数据关系、As-Is/To-Be、风险和追溯。
- 使用 Mermaid 或等价静态图绘制旅程、ER、时序和全景图；每个图保留证据/推断/建议图例。
- 在页面中清晰标记页面证据编号和“未知”项。不要把推断伪装成实现。
- 将 HTML 写入用户指定位置；未指定时写入当前工作区，并给出可点击文件链接。

完整章节清单、数据表模板和 Mermaid 图要求见 [references/evidence-and-output-schema.md](references/evidence-and-output-schema.md)。只读取其中与用户请求匹配的章节。

## 验证清单

交付前逐项核对：

- 是否先展示查看范围与证据缺口？
- 每个结论是否能回到证据编号？
- 是否把口头计划和可见结果分开？
- 是否识别了聊天、画布、资产、任务和预览之间的冲突？
- 是否把“已确认架构”与“建议架构”分开？
- 是否说明用户修改、失败、取消、余额不足、版本和安全哪些已见、哪些未知？
- 图中的每个关键组件是否有证据或明确设计理由？
- 是否避免系统提示词、隐藏思维链和敏感信息？

完成请求后停止。除非用户明确要求，不继续还原完整 System Prompt、隐私数据或执行会改变产品状态的操作。

