---
name: lvmaiya-product-deconstruction
description: "Evidence-led reverse engineering for any digital or AI product from screenshots, recorded UI flows, websites, prototypes, local artifacts, and official documentation. Use when Codex needs to analyze or audit a product's user journey, workflow, agents, tools, global context, assets, model choices, data flow, market position, state inconsistencies, risks, or To-Be design; especially when the deliverable is a cited HTML report, journey map, agent I/O contract, scorecard, or architecture diagram."
---

# 绿麦芽的万能产品拆解大法宝

以可复核证据为先，拆解任何数字或 AI 产品的体验、交付、Agent、工具、数据、资产与架构。目标不是替产品“脑补实现”，而是让产品、设计和工程团队明确区分：**页面已经证明的系统、由证据推导的假设，以及值得验证或建设的方案**。

## 先定边界

- 默认只读。浏览页面、查看截图、读取本地文件和官方公开资料可以；不得发送消息、触发生成、发布、删除、购买、充值、重试或覆盖资产，除非用户单独授权。
- 不读取或输出 Cookie、Token、密码、鉴权头、个人敏感信息或其他不在任务范围内的数据。
- 不把“思考完成”“规划完成”当作隐藏思维链；它们只能作为面向用户的判断摘要。
- 不把 Agent 的计划、自然语言宣告或流程图，当作工具已执行。只有可见结果卡、资产变化、任务/状态变化、错误、可复核运行结果或官方文档才能确认动作。
- 不把前端状态当作后端真相。聊天、画布、资产页、预览、任务面板不一致时，逐项记录冲突，不任选一方为事实。
- 只能把官方资料、产品页面和用户提供材料当作外部证据；不要用第三方猜测证实架构。
- PDF、旧报告中的架构图、字段、工具名和 Prompt 示例，除非另有原始页面或官方资料佐证，默认是**分析者的方案/假设**，不是被拆产品的事实。

## 证据等级与写作规则

为每个非平凡结论贴一个标签，并紧邻列出证据编号、页面原文、按钮、资产、错误或官方链接。

- `【已确认】`：页面、官方文档或可复核产物直接支持。
- `【合理推断】`：由两条或以上事实推导；写出推导链和替代解释。
- `【建议设计】`：为修复风险、提升可运营性或补齐能力提出；不得暗示已存在。
- `【未知】`：当前材料不足；写出需要查看什么才能确认。

分开维护“证据台账”和“假设/设计台账”。不要把截图文件名、整理者注释或 Agent 名称以外的元数据当作唯一事实；优先记录实际可见的内容。

## 工作流

### 1. 建立范围、证据与测试卡

1. 列出全部输入：截图、录屏、网页、原型、日志、文档、CSV、已有报告与官方资料；分配稳定编号，如 `S01`、`P03`、`D02`。
2. 每项记录时间顺序、可见范围、遮挡/缺页、是否原始材料。逐张以足够清晰的倍率视觉检查截图；查看聊天、表单、按钮、画布、资产、任务、预览和错误。
3. 页面可访问时，仅以只读方式检查任务标题、导航、模型选项、表单、Agent 身份、资产卡、历史/版本、预览、错误、余额和状态；不要执行有副作用的动作。
4. 对实际体验任务先建立**测试卡**：目标用户、任务/原始提示、控制输入（时长、比例、风格、素材等）、入口/模式、成功判定、检查点、可见失败分支。单个测试场景不能泛化为产品全貌。
5. 记录未能访问或未能确认的证据，不以经验补齐。详见 [references/evidence-and-output-schema.md](references/evidence-and-output-schema.md)。

### 2. 还原用户旅程与体验评分

按时间顺序输出：

| 阶段 | 用户目标与动作 | 页面反馈 | 决策/分支 | 状态与资产变化 | 阻力/情绪 | 证据 |
|---|---|---|---|---|---|---|

至少检查：初始需求、参数/风格、满意/修改、方案选择、暂停/取消/返回、正常生成、依赖阻断、模型失败、余额不足、预览/编辑/下载/发布。没有证据的分支必须写 `【未知】`。

用户旅程图只使用 **用户**、**产品界面**、**系统结果** 三条泳道；判断点用菱形；正常主流程、用户纠偏流程、失败/中断流程分别可见；节点写入证据编号。

若需要评估或横向比较，先公开评分维度、权重、计分口径和未评分原因。可使用下列**可调整的默认维度**，但不是所有产品的固定标准：端到端交付（40%）、用户共创与可控性（20%）、多 Agent/流程协同（25%）、模型/数据/资产基础（15%）。没有证据的项标“未评分”，不能当作 0 分。

### 3. 识别实际出现的 Agent 与工作模式

- 只记录页面、官方资料或可复核产物支持的 Agent/系统角色；不要预设固定数量，也不要把任务容器自动等同于主控 Agent。
- 对每个角色记录：首次可见、触发者、接替谁、给谁交接、是否再次出现、哪些通常角色没有出现。
- 识别可见的工作模式（例如对话式、托管/自动式、专业工作台）；未在页面出现的模式不能默认存在。若模式可切换，检查切换是否影响确认点、可编辑性或执行状态。
- 对每个 Agent 建立 I/O 契约。输入逐项检查六类：用户当前输入、用户长期信息、项目全局上下文、上游 Agent 输出、平台公共资产/知识、工具/运行时结果。
- 长期用户信息、Face ID、音色、历史资产、套餐、权限等只有可见证据才能标事实；否则为推断或未知。

每张契约卡必须使用：

1. 核心目标
2. 触发条件
3. 输入信息（注明六类来源与证据等级）
4. 可观察判断（不还原隐藏思维链）
5. 调用工具（功能命名；不是官方工具名时明确标注）
6. 输出信息（用户回复、页面组件、结构化字段、视觉/音视频资产、状态、下游任务）
7. 全局上下文读写（字段、读/写、生产者、消费者、更新时机、证据）
8. 完成条件
9. 异常与重试
10. 尚未确认的问题

### 4. 拆工具、资产、状态与全局上下文

- 使用功能名描述未公开工具，如“读取项目上下文”“生成角色主图”“检查余额”“写入分镜引用”；紧跟“功能命名，并非官方工具名”。为每项记录关联 Agent、可见输入、可见结果、执行结论（已确认/仅计划/未知）。
- 根据证据核查 `ProjectMeta`、Brief/Script、Character/Scene/Prop、Storyboard、Asset/AssetVersion、Workflow/Task、Approval、Billing、Error/Event 等对象；这些是**分析模板，不代表真实字段或表**。
- 对每个资产记录生产者、版本、项目归属、被谁引用、完成度、预览/下载能力和冲突。不可见的 ID、存储地址和历史版本不要编造。
- 数据流必须明确生产者、消费者、读写、更新时机、稳定引用和失效规则。重点找：同一数据多版本、只写不读/只读不写、上游修改后下游是否 stale、口头完成但状态未更新。
- 将“已观察到的数据流”与“建议的全局上下文/状态机”分开画。建议设计可采用项目元数据、实体资产库、引用图、任务运行状态、用户确认、计费与错误事件的分层模板，但必须标注建议。

### 5. 按需拆产品、商业与技术架构

只有用户要求完整拆解时，依次覆盖：

1. 证据来源、测试范围与缺口
2. 产品定位、目标用户、核心交付物与功能域
3. 用户旅程、体验问题与评分（如适用）
4. 五条并列端到端流：用户交互、Agent/流程控制、工具调用、数据/全局上下文、图片/视频/音频资产
5. Agent—工具—上下文、数据生产者—消费者、资产引用与状态冲突
6. 知识与公共资产：项目数据、专业知识、模型能力、风格/提示词、安全/版权/计费、私有资产隔离
7. 模型接入与路由：只陈述页面或官方资料可见模型；模型路由、成本、时长、失败率无证据时标未知
8. 分层架构、数据实体/ER、时序、产品全景图，以及 As-Is、To-Be、风险、证据追溯和待验证问题
9. 市场、竞品或商业模式只在用户要求且使用新鲜、可引用来源时分析；可见定价页仅能证明当前展示的价格/套餐，不能证明收入或使用规模。

架构图图例必须区分：实线 `【已确认】`、虚线 `【合理推断】`、点线/特殊色 `【建议设计】`。箭头标注调用、读取、写入、事件、确认、资产引用或状态更新；把冲突和未完成校验画成问题节点。

### 6. 生成交付物

当用户要求 HTML、可视化页面或完整报告时，交付普通浏览器可打开的独立 HTML；其他情况严格遵从用户指定格式，不因本 Skill 默认创建文件。

- 顶部给一句话执行摘要、证据范围、测试卡和图例。
- 用 HTML 表格呈现旅程、评分、Agent 契约、工具、上下文、数据关系、As-Is/To-Be、问题与追溯。
- 使用 Mermaid 或等价静态图绘制旅程、Agent I/O、ER、时序和全景图；每图保留证据/推断/建议图例。
- 对每个问题采用“问题 → 证据 → 可能原因（推断）→ 下一步验证 → 产品/技术机会”的链路。
- 将 HTML 写入用户指定位置；未指定时写入当前工作区，并给出可点击文件链接。

完整章节清单、测试/评分模板、数据表模板和 Mermaid 图要求见 [references/evidence-and-output-schema.md](references/evidence-and-output-schema.md)。只读取与当前请求匹配的章节。

## 验证清单

- 是否先展示查看范围、测试边界与证据缺口？
- 每个结论能否回到证据编号？是否分离了页面事实、推断、建议和未知？
- 是否把口头计划、页面公开判断摘要和可见执行结果分开？
- 是否实际检查了聊天、画布、资产、任务和预览之间的冲突？
- 是否清晰说明了用户修改、失败、取消、余额不足、版本和安全哪些已见、哪些未知？
- 若评分，权重和未评分原因是否透明？
- Agent 契约是否写明输入来源、读写边界、交接前置条件、失败/重试和完成条件？
- 是否把架构模板或 PDF 中的建议与产品实际实现分开？
- 是否避免完整 System Prompt、隐藏思维链和敏感信息？

完成请求后停止。除非用户明确要求，不继续还原完整 System Prompt、隐私数据或执行会改变产品状态的操作。
